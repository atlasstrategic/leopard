# Development status

## Playable now

Milestone-B local Three.js twin-engine handling, wind/handling tuning and scenario current, three cameras, Raymarine-inspired instruments and a fictional solid berth. Milestone-C batches: bounded contact/telemetry recorder and JSON/CSV exports; independent delayed fender crew with local cushioning; B01/B02 bow/stern mooring with tension-only fairlead forces, load warnings and failure; live approach → securing → secured states. Together with the batches below, these now form the **complete fuel mission loop** (holding → clearance → approach → secured → service → departure → debrief, with restart checkpoints), tracked in issue #1.

**Alongside correction:** after the first successful line attachment, the mint arrival target switches to a configurable amber alongside whole-hull envelope. The old 1.2 m approach-centre requirement no longer governs securing. Both lines need limited slack, safe load and idle crew; gentle correctly covered east-quay fender contact can count (hard/bare/wrong-obstacle contacts cannot). The active target stays alongside after both lines are released; Retry resets it. Take in/Ease adjusts paid-out length in bounded 0.25 m tasks at 0.12 m/s with neutral/speed/load/length limits, Stop, feedback and events. The vessel is never snapped toward a target.

**Show me:** separate calm-water default-boat run with explanatory steps for fenders, momentum/braking, arrival hold, first-line target switch, paired gradual tending and settling. It uses ordinary session physics, crew commands and objectives; no pose injection. Pause/Repeat/Take over/Return to practice; the original attempt remains preserved, paused. This shows one suitable bow-first method for these conditions, not universal seamanship.

**Harbour traffic (issue #1, step 1):** a scripted 12 m monohull starts at the fuel berth and, when released by the holding stage, backs off the quay and leaving to the south-west. It stops for the player's boat instead of pushing through. The contact solver now supports moving capsule obstacles using relative velocity; fixed-box contacts are numerically unchanged. Show me runs without traffic.

**Holding and clearance (issue #1, step 2):** attempts now start in a holding phase with harbour-radio announcements. A 5 s countdown runs while the boat's centre is inside the holding area south-east of the quay (resetting if it leaves); the monohull then departs, and the berth is called clear once the monohull has left the fuel berth and approach lane, which starts the existing approach. Early entry into the fuel zone costs +10 s per entry. Contact with the monohull on an uncovered hull point fails the mission (simulation stops until Retry); fender-covered contact is a normal penalty. Export schema version 2, scenario version 5.

**Fuel service (issue #1, step 3):** once first secured, a checklist runs engines off → diesel → fuel (180 L, accelerated) → pay → engines on. Out-of-order steps and petrol are refused with explanations. Engines off means no thrust and counts as neutral for securing. Losing secured stops fuelling (litres kept) and blocks further steps until re-secured; engines may be restarted at any time for safety. The Session option `traffic` is now `mission`, covering traffic, holding and service. Development builds expose `window.__leopard.lab` for scripted browser checks.

**Departure (issue #1, step 4):** the south training barrier is now a breakwater with an 18 m entrance marked by red (west) and green (east) lights, with training barriers enclosing the water outside. Completing the service starts the departure: let go both lines (no longer unsecures), clear the quay, and exit between the lights; crossing the gate in the port half of the channel costs +5 s. Crossing the gate completes the mission and stops the simulation. The monohull now exits through the entrance on its starboard side. Scenario version 6.

**Debrief and score (issue #1, step 5):** the session records debrief metrics (closest monohull gap, peak speed near the berth, countdown resets, fenders on arrival, lever changes, refused commands, lines let go under load). When the mission completes or fails, a debrief dialog shows a 0–100 score using the plan's 40/25/20/15 weighting, per-category deductions, key figures, penalty reasons, one tip from the largest weighted loss, and an overhead SVG trace of both tracks with contact points. Failed missions are not scored. Weights and thresholds are provisional (`scoreConfig`).

**Restart checkpoints (issue #1, step 6):** Approach (berth called clear) and Departure (service complete) checkpoints are saved automatically. Restart from either via buttons in the objective panel and debrief: the exact saved moment is restored as a new attempt continuing the earlier log; Retry still restarts the whole mission. The debrief counts restarts (not scored). This completes milestone C's mission loop.

## Verified on this machine

- `npm test`: **94 passed**, including checkpoint save on clearance, exact restore with carried-over log and new attempt, repeatable restarts, departure checkpoint with lines on, restart after failure, fallback to a full retry, and the debrief restart count. Also clean-run scoring, weighted totals and tip choice, proportional deductions, unscored failure, penalty reasons and tracks, session metric recording and retry reset. Also entrance geometry, service → departure, letting go without unsecuring, starboard-side exit completing the mission, port-side penalty, no completion outside the departure phase, a solid breakwater outside the gap, and the monohull exiting on its starboard side. Also the fuel service order and refusals, petrol refusal, engines-off thrust and securing, fuelling interrupted by losing secured then resumed, pause and retry. Also holding countdown and reset, clearance timing, early-entry penalties, line refusal before clearance, mission failure on bare monohull contact versus fender-covered penalty, radio briefing, and 30/60/144 Hz equivalence of traffic, progress and radio. Also monohull route clearance, moving-hull relative contact, penalties, yielding without contact, retry and 30/60/144 Hz traffic equivalence, plus the existing along-quay screenshot regression, real contact samples, line-tending safety/limits, demo completion and frame-rate equivalence (30/60/144 Hz), preservation, takeover and fail-stop.
- `npm run typecheck`, `npm run build`, `git diff --check`: passed. Vite reports an informational >500 kB uncompressed chunk warning (~153 kB gzipped JS).
- Linux Chrome 150 WebGL 2 browser **UI-only** “Show me”: completed normal secured state at **81.0 simulation seconds**, **zero impact penalties**; paused/resumed, read-only lesson controls, takeover and Ease, JSON export, restored the saved practice display, repeated cleanly. Checked 1440×1000 and 1100×760; no captured console/page errors. Evidence: `artifacts/show-me-tending.png`, `show-me-complete.png`, `show-me-compact.png`, `show-me-recording.json`; script `tools/browser-show-me.mjs`. Browser check was run before the small terminal-stage guard added afterward; that guard is covered by the final automated run.
- Chrome (Playwright) checkpoints: after the berth was called clear (55.1 s) the Restart from · Approach button appeared with its tooltip; after driving away, clicking it restored the saved position, APPROACH phase, radio call and neutral levers as attempt 2. No console errors besides a missing favicon. Evidence: `artifacts/checkpoint-restart.png`.
- Chrome (Playwright) debrief: full mission with the holding area and berth reached via the dev handle (so the trace shows those jumps), fenders, service and lines through the real buttons, and a scripted lever controller out through the starboard half of the entrance. The debrief opened automatically: 84 (Good), one breakwater contact as the tip, figures, penalty reason, and a trace with both tracks and the contact point. Close, reopen from the objective panel and Retry all worked; a bare-hull monohull contact produced the unscored failure debrief. The lever-change count (705) reflects the script adjusting levers every 60 ms, not human input. This check found and fixed a crash in the departure result text when the service time was missing, which also stopped the render loop; normal play always sets it. Evidence: `artifacts/debrief.png`, `debrief-failed.png`.
- Chrome (Playwright) departure: service completed through the real checklist buttons, both lines let go with the Crew & lines buttons, BRG EXIT guidance, dashed gate and red/green lights, then a scripted lever controller steered out. It brushed the west breakwater twice (+5 s each; the scripted steering, not a game fault) and crossed in the port half, so this run exercised the +5 s channel-side penalty and the mission-complete screen and radio (278.7 s, +15 s). The starboard-side path is covered by tests. No console errors besides a missing favicon. Evidence: `artifacts/departure-entrance.png`, `departure-complete.png`.
- Chrome (Playwright) fuel service through the real checklist buttons (secured state set up via the dev handle): early fuelling and petrol refused with explanations, ENGINE OFF lever readouts, litres counting, radio calls, service complete at 14.5 s with no penalties, buttons disabled afterwards. Checked 1440×1000 and 1100×760 (objective panel scrolls for the last line). No console errors besides a missing favicon. This run found and fixed an early-entry check that treated a clearance at time 0 as "not cleared". Evidence: `artifacts/service-fuelling.png`, `service-complete.png`, `service-compact.png`.
- Chrome (Playwright) holding run driven with keyboard input: radio briefing, BRG HOLD guidance, countdown in the holding area, departure call, berth called clear at 92.0 s with no contacts or penalties, approach objective active. Checked 1440×1000 and 1100×760 radio layout. Only console error: missing favicon (plus errors from the test script's own synthetic key events, not the app). Evidence: `artifacts/holding-countdown.png`, `holding-cleared.png`, `holding-compact.png`.
- Chrome (Playwright) visual check of the monohull departing at 7.8 s and turning out at 50 s, no console errors besides a missing favicon: `artifacts/monohull-departing.png`, `artifacts/monohull-turning-out.png`.
- Earlier Chrome smoke/contact/fender/mooring checks: UI controls/cameras, deliberate unprotected bow impact and local coverage, exports/history, and original approach/attach/secure/release at **58.3 s**, zero penalties. These earlier results predate the alongside rule and should **not** be interpreted as a fresh browser check of manual alongside securing. Files and scripts in `artifacts/` and `tools/`.

## Hosting

Public GitHub repository `atlasstrategic/leopard`, deployed at **https://atlasstrategic.github.io/leopard/**. The Pages Action gates deployment of `dist/` on tests, typecheck and build. First run completed successfully; the public index and hashed JS/CSS returned HTTP 200 with correct types. Live Chrome 150 loaded the game, opened Show me, and paused the lesson without captured console/page errors. This verifies a deployment smoke test, **not** a complete browser playthrough on Pages.

## Assumptions and next work

September 2024 Leopard 42 dimensional/equipment baseline, not a verified 2026 configuration. Planar low-speed dynamics, visual hull/rig, thrust/drag, line elasticity/failure, fitting capacity, crew rates and fender coverage are **provisional game parameters**, not calibrated or certified safety limits. Contact load is summed solver impulse per fixed tick divided by tick duration, **not physical peak force or damage**. Two lines are not full real-world mooring; no springs/breast lines, rope wrapping/chafe, simulated crew travel, waves, prop effects, depth or moving traffic. Browser feel and real hardware performance have not been validated by a skipper; no Windows/macOS/mobile acceptance.

Next: playtest and calibrate handling and the provisional score thresholds with experienced operators, then milestone D (a recognisable Croatian harbour and boat assets) and milestone E (browser hardening). No actual Croatian geography or real fuel-service claims yet. See `README.md` for controls, units and verification procedures.
